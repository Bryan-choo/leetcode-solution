import java.math.BigInteger;
import java.util.Arrays;
import java.util.LinkedList;
import java.util.List;
import java.util.Scanner;
import java.util.stream.Collectors;

public class getMaxIters {


    public static void main(String[] args) {

//
//        Scanner sc = new Scanner(System.in);
//
//        int n = Integer.valueOf(sc.nextLine());
//        BigInteger nb = new BigInteger(String.valueOf(n));
//        BigInteger one = new BigInteger("1");
//
//
//        String[] var = sc.nextLine().split(" ");
//
//        List<BigInteger> list = Arrays.stream(var).map(r -> {
//            BigInteger integer = new BigInteger(r);
//            return integer;
//        }).collect(Collectors.toList());
//
//        int k = 0;
//
//        list = list.stream().sorted((a, b) -> {
//            return a.compareTo(b);
//        }).collect(Collectors.toList());
//
//        while (list.get(list.size()-1).compareTo(nb) > 0) {
//            k++;
//
//            BigInteger var1 = list.get(list.size()-1).subtract(nb);
//
//            list.stream().forEach(r -> {
//
//                r.add(one);
//
//            });
//
//            list.set(list.size()-1, var1);
//            list = list.stream().sorted((a, b) -> {
//                return a.compareTo(b);
//            }).collect(Collectors.toList());
//        }
//
//
//        Scanner sc = new Scanner(System.in);
//        String v = sc.nextLine();
//
//        BigInteger var = new BigInteger(v);
//        BigInteger var1 = new BigInteger(v);
//        String ans = var.multiply(var1.subtract(new BigInteger("1"))).subtract(new BigInteger("1")).toString();
//        double var = Double.valueOf(sc.nextLine());
//        int ans = (int) (var*(var-1)-1);
//        System.out.println(ans);
//
//        System.out.println(k);

    }
}
